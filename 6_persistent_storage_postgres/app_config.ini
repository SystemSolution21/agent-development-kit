[Application]
AppName = Persistent Agent

[Database]
# Use 'postgresql' or 'sqlite'
DB_TYPE = postgresql

# --- SQLite settings (used if DB_TYPE is 'sqlite') ---
DatabaseName = my_agent_data.db

# --- PostgreSQL settings (used if DB_TYPE is 'postgresql') ---
# For production, it is recommended to set these as environment variables.
PG_HOST = localhost
PG_PORT = 5432
PG_USER = postgres
PG_PASSWORD = 12345678
PG_DBNAME = postgres

UserID = john_doe
UserName = John Doe